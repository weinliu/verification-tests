kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: <name>
  namespace: <namespace>
  annotations:
    volume.beta.kubernetes.io/storage-class: <sc-name>
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      # ex: 500Mi,1Gi
      storage: <size>

