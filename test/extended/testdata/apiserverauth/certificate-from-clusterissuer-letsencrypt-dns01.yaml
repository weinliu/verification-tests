apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: certificate-from-dns01
spec:
  secretName: certificate-from-dns01
  dnsNames:
  - DNS_NAME
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-dns01
