apiVersion: batch/v1
kind: Job
metadata:
  name: fail-job
spec:
  template:
    spec:
      containers:
      - name: fail
        image: busybox
        command: ["sh", "-c", "exit 1"]
      restartPolicy: Never
  backoffLimit: 0
