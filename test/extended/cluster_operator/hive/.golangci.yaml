run:
  timeout: 10m

output: {}

linters:
  enable:
  - ginkgolinter

issues:
  # Fix found issues (if it's supported by the linter).
  fix: true
  # Show only new issues created after a git revision
  new-from-rev: master
  exclude-rules: []
